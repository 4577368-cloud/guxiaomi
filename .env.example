# Local API address used by build-time env.js generation.
# In local static preview, the frontend also falls back to http://localhost:8123.
ANALYSIS_API_BASE=http://localhost:8123

# LLM services used by stock analysis, deep diagnosis, and Ziwei chat.
# Frontend sends only model1/model2/model3; secrets stay on the API server.
LLM_DEFAULT_MODEL_KEY=model2

LLM_MODEL1_LABEL=MiniMax
LLM_MODEL1_BASE_URL=
LLM_MODEL1_API_KEY=
LLM_MODEL1_MODEL_ID=

LLM_MODEL2_LABEL=Gemma
LLM_MODEL2_BASE_URL=
LLM_MODEL2_API_KEY=
LLM_MODEL2_MODEL_ID=

LLM_MODEL3_LABEL=Deepseek
LLM_MODEL3_BASE_URL=
LLM_MODEL3_API_KEY=
LLM_MODEL3_MODEL_ID=

# Optional market/news enrichments. Leave blank if you do not have keys.
ALPHA_VANTAGE_API_KEY=
GNEWS_API_KEY=

# Optional proxies for server-side data/news fetching.
GTIMG_HTTP_PROXY_TEMPLATE=
RSS_PROXY_URL=

# Optional CORS allowlist for deployed API, comma-separated.
ALLOWED_ORIGINS=

# Vercel Postgres / Neon connection string (free tier)
# Format: postgres://user:password@host:port/database?sslmode=require
POSTGRES_URL=

# --- 定时刷新（持仓/关注行情快照 + 预测） ---------------------------------
# 保护 /api/cron/refresh 的密钥。GitHub Actions 用 Authorization: Bearer <CRON_SECRET>
# 或 ?token=<CRON_SECRET> 调用（见 .github/workflows/scheduled-refresh.yml）。
# 线上（Vercel）必须设置，否则该端点会拒绝以防被滥用。
CRON_SECRET=

# 仅常驻进程（Render/Railway/本地 uvicorn 长开）用：置 1 启动内置调度器；Vercel 上会自动忽略。
ENABLE_SCHEDULER=
# 内置调度器间隔（分钟，最小 5），默认 60。
SCHEDULER_INTERVAL_MIN=60
# 内置调度器执行的任务，默认 prices,predictions（可只写 prices）。
CRON_REFRESH_TASKS=prices,predictions
# 预测刷新的组合：period-trend-type，逗号分隔。留空=全部 18 组（周期×方向×类型）。
# 例：只刷看涨股票的日/周/月 → 0-0-0,1-0-0,2-0-0
CRON_SCREENER_COMBOS=
